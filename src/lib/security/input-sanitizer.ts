/**
 * Input sanitizer adapter / alias for backwards compatibility.
 * Re-exports from @/lib/sanitization/sanitize.
 */

export {
  escapeHtml,
  sanitizeCriticalText,
  sanitizeDescription,
  sanitizeForMeta,
  sanitizeText,
} from "@/lib/sanitization/sanitize";
