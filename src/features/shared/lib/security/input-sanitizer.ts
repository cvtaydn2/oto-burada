/**
 * Input sanitizer adapter / alias for backwards compatibility.
 * Re-exports from @/features/shared/lib/sanitize.
 */

export {
  escapeHtml,
  sanitizeCriticalText,
  sanitizeDescription,
  sanitizeForMeta,
  sanitizeText,
} from "@/features/shared/lib/sanitize";
