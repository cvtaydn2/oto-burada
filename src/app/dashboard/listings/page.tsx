import { DashboardPlaceholder } from "@/components/shared/dashboard-placeholder";

export default function DashboardListingsPage() {
  return (
    <DashboardPlaceholder
      eyebrow="İlanlarım"
      title="İlan yönetim alanı hazırlanıyor"
      description="Kullanıcının kendi ilanlarını görüntüleme, durum görme ve düzenleme akışı daha sonraki görevde bu alanın içine yerleşecek."
    />
  );
}
